// ConsoleApplication1.cpp : Este archivo contiene la función "main". La ejecución del programa comienza y termina ahí.
//

#include <iostream>
#include <algorithm>


int main() 
{

	// Declaramos las variables que usaremos int numl, num2, a, b, res;

	int num1 = 0, num2 = 0, var1 = 0, var2 = 0, answer = 0;

	// Pedimos al usuario que ingrese los numeros 
	
	std::cout << "Ingrese el primer numero: " << std::endl;
	std::cin >> num1;

	std::cout << "Ingrese el segundo numero: \n"; // Seleccionamos el mayor y el menor y los

	std::cin >> num2;

// asignamos a las variables "a" y "b"

	a = std::max(num1, num2);

	b = std::min(num1, num2);

// Creamos el ciclo que realizará las iteraciones

do (

	res = b;

b = atb;

res;

} while (b != 0);

// Mostramos el resultado en pantalla cout << "El M.C.D. entre "<<numl<<" y "<<num2<<" es: "<<<res;

return 0;